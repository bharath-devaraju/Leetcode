#include <bits/stdc++.h>

using namespace std;

class Solution {
   public:
    int change(int amount, vector<int>& coins) {
        int numCoins = coins.size();
        vector<vector<int>> dpTable(numCoins, amount);
    }
};

int main() { return 0; }
